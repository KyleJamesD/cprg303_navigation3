import React from "react";
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
  } from 'react-native';

  function AddTransaction({navigation} : {navigation : any}) : React.JSX.Element {
    return (
            <View>
                <Text>This is the Page to Add Transaction</Text>

            </View>

    );
  }

  const style = StyleSheet.create({
    container:{

    },
    item: {
        
    },
  }) 

export default AddTransaction;